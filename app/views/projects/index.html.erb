		<h1>All the projects: </h1>
		<br>
		<button><a href="http://localhost:3000/projects/new">Create a new Project</a></button>
		<hr>
		<% @projects.each do |project| %>
			<li>
				Title: <%= link_to(project.title, project_path(project.id)) %> 
				<%if project.description != nil%>
					<%=" ==>  #{project.description}" unless project.description.nil? %>
				<%end%>
			</li>
		<%end%>
		<hr>
		<img src="http://media0.giphy.com/media/pn8ZlwPvVClyM/giphy.gif" alt="">